Work in progress. Two goals:

  1. Enable access to SQLite databases from Go.
  2. Design a generic database API for Go that
     supports multiple RDBMSs.

Both of these are partially accomplished. See
core.go and error.go for the former, dbapi.go
for the latter.
